version: "2.4"

services:
  postgresql:
    image: "postgres:11.5"
    ports:
      - "15432:5432"
    volumes:
      - "./postgresql/data:/var/lib/postgresql/data"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: sonis
    user: ${USER_ID}